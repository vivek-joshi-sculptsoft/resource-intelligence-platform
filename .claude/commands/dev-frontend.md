Start the frontend Vite dev server with hot-reload.

Steps:
1. `cd frontend && npm run dev`

This runs Vite dev server with HMR. App available at http://localhost:5173. API requests proxy to http://localhost:8000 via vite.config.ts.
